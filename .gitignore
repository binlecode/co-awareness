CoAwareness
# The launcher's build temp, renamed over the binary on success. Listed explicitly — a
# `CoAwareness*` glob would swallow CoAwareness.swift, the only source of truth.
CoAwareness.new
tmp/
.wrangler/
.claude/settings.local.json


